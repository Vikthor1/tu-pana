name = "tupana-gemini-proxy"
main = "src/index.js"
compatibility_date = "2024-01-01"

# GEMINI_API_KEY must be set as a Cloudflare secret — never in this file.
# To set locally: add it to .dev.vars (gitignored).
# To set in production: wrangler secret put GEMINI_API_KEY
