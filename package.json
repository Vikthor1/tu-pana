{
  "name": "tupana-test-server",
  "version": "1.0.0",
  "description": "Local mock server and deterministic regression harness for Tu Pana de Escritura",
  "main": "test-server.js",
  "private": true,
  "engines": {
    "node": ">=24.19.0 <25"
  },
  "scripts": {
    "start": "node test-server.js",
    "dev": "node --watch test-server.js",
    "test": "node run_test_suite.mjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.3"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
